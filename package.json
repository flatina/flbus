{
  "name": "@flatina/flbus",
  "version": "0.2.7",
  "private": false,
  "description": "Agent-to-agent, human-gated message bus for Claude Code sessions — CLI for the flbus Claude Code plugin. After `npm i -g`, install the plugin (`claude plugin install flbus@flatina`) and wire the statusLine per the README; `flbus doctor` verifies.",
  "keywords": [
    "claude-code",
    "claude-code-plugin",
    "agent",
    "message-bus",
    "ipc",
    "cli"
  ],
  "homepage": "https://github.com/flatina/flbus#readme",
  "repository": {
    "type": "git",
    "url": "https://github.com/flatina/flbus.git"
  },
  "bin": {
    "flbus": "dist/flbus.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build src/flbus.ts --target=node --format=cjs --outfile dist/flbus.js",
    "prepare": "bun run build"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0"
  }
}
