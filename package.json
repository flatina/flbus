{
  "name": "@flatina/flbus",
  "version": "0.2.3",
  "private": false,
  "bin": { "flbus": "dist/flbus.js" },
  "files": ["dist"],
  "scripts": {
    "build": "bun build src/flbus.ts --target=node --format=cjs --outfile dist/flbus.js",
    "prepare": "bun run build"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0"
  }
}
